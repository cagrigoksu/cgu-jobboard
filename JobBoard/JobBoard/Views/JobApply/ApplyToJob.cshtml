@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@using JobBoard.Enums
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model JobBoard.Models.View.JobApplyViewModel
<form method="post" asp-action="Apply" asp-controller="JobApply"  enctype="multipart/form-data">

    <input hidden asp-for="JobId" value="@Model.JobId" />
    <input placeholder="Job Title" type="text" name="title" asp-for="Title" value="@(String.IsNullOrEmpty(Model.Title)? "" : Model.Title)" readonly>
    <br><br />
    <select asp-for="LevelId" disabled>
        @{
            foreach (var enumItem in Enum.GetValues(typeof(JobLevelEnum)))
            {
                <option value="@((int)enumItem)" selected="@(Model.LevelId == (int)enumItem)"> @(enumItem) </option>
            }
        }
    </select>
    <br><br />
    <input placeholder="Country" asp-for="Country" value="@(String.IsNullOrEmpty(Model.Country)?"": Model.Country)" readonly/>
    <br><br />
    <input placeholder="City" asp-for="City" value="@(String.IsNullOrEmpty(Model.City)?"": Model.City)" readonly/>
    <br><br />
    <textarea asp-for="Description" rows="4" cols="50" readonly>@(String.IsNullOrEmpty(Model.Description) ? "" : Model.Description)</textarea>

    <br><br />
    <h3>Your Resume</h3>
    <br /><br />
    <input type="file" asp-for="CV"/>
    <br><br />
    @* <input type="button" value="edit this post" asp-route-id="@Model.JobId" asp-route-edit="@true" onclick="@("window.location.href='"+@Url.Action("EditJobPost", "Poster", new {id = Model.Id, edit=true })+"'")" /> *@
    @* *@


    <input type="submit" value="Apply" />
</form>





